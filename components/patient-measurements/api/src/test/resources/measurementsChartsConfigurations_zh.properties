
# Override each setting with valid values
charts.tested.configurations.valid.chartTitle=1至3岁的体重
charts.tested.configurations.valid.topLabel=年龄（月）
charts.tested.configurations.valid.bottomLabel=年龄（每月）
charts.tested.configurations.valid.leftLabel=重量（公斤）
charts.tested.configurations.valid.rightLabel=重量（公斤）

# Most of the settings have valid default values, only specify the mandatory ones
charts.tested.configurations.defaulted.chartTitle=年龄体重，出生至20岁
charts.tested.configurations.defaulted.leftLabel=重量（公斤）

# Check that the left<->right and top<->bottom labels are duplicated when only one of each pair is present
charts.tested.configurations.fallbackBottomRight.chartTitle=标题
charts.tested.configurations.fallbackBottomRight.leftLabel=测量标签
charts.tested.configurations.fallbackBottomRight.topLabel=年龄标签
charts.tested.configurations.fallbackTopLeft.chartTitle=标题
charts.tested.configurations.fallbackTopLeft.rightLabel=测量标签
charts.tested.configurations.fallbackTopLeft.bottomLabel=年龄标签

# Some broken or exotic values, which will be ignored, although the global configuration is still valid
charts.tested.configurations.broken.chartTitle=年龄权重，bģ至2 0岁
charts.tested.configurations.broken.leftLabel=重量（公斤）

# Errors that will be checked and reported, making these settings invalid
# This includes missing mandatory values

# This isn't listed, so it should be ignored
charts.tested.configurations.extra.chartTitle=年龄体重，出生至20岁
charts.tested.configurations.extra.leftLabel=重量（公斤）




charts.invalid.configurations=无效
charts.invalid.configurations.invalid.lowerAgeLimit=36
charts.invalid.configurations.invalid.upperAgeLimit=12
